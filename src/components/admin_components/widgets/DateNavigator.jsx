const DateNavigator = () => {
  return (
    <div className="date-nav">
      <button>◀</button>
      <input type="date" />
      <button>▶</button>
    </div>
  );
};

export default DateNavigator;
